import React from 'react'
import Layout from '../components/layout'

const NotFound: React.FC = () => {
    return (
        <Layout>
            <div>該当のページは存在しません。</div>
        </Layout>
    )
}

export default NotFound
